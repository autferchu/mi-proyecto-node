// src/services/userService.js

// Simulamos una base de datos pequeña
const usuariosBD = [
  { id: 1, nombre: 'Ana' },
  { id: 2, nombre: 'Carlos' }
];

const obtenerTodosLosUsuarios = () => {
  // Aquí en el futuro harías la consulta a MongoDB, MySQL, etc.
  return usuariosBD;
};

module.exports = {
  obtenerTodosLosUsuarios
};
// src/services/userService.js

// Simulamos una base de datos pequeña
const usuariosBD = [
  { id: 1, nombre: 'Ana' },
  { id: 2, nombre: 'Carlos' }
];

const obtenerTodosLosUsuarios = () => {
  // Aquí en el futuro harías la consulta a MongoDB, MySQL, etc.
  return usuariosBD;
};

module.exports = {
  obtenerTodosLosUsuarios
};